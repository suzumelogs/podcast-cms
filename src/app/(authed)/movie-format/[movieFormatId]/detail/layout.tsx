import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Movie format details',
}

export default function Page({ children }: { children: React.ReactNode }) {
  return children
}
